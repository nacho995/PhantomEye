version: '3.8'
services:
  db:
    image: postgres:17
    environment: 
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=PhantomEye
    ports:
        - 5433:5432
    volumes:
        - phantom_data:/var/lib/postgresql/data
volumes:
  phantom_data:
    